apiVersion: v1
kind: Service
metadata:
  name: string-reverse-svc
  labels: 
    app: string-reverse
spec:
  selector:
    app: string-reverse
  sessionAffinity: None
  ports:
  - port: 8090
    protocol: TCP
    targetPort: 8080
  type: ClusterIP